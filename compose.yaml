services:
  backend:
    build:
      context: ./Backend/TaskTrackerAPI
      dockerfile: Dockerfile
    ports:
      - "80:8080"
    volumes:
      - ./Backend/TaskTrackerAPI/tasktracker.db:/app/tasktracker.db
