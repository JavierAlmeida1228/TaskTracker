name: Deploy

on:
  workflow_run:
    workflows: ["CI Pipeline"]
    types:
      - completed

jobs:
  deploy:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy to class server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: 38.46.230.15
          username: Jalmeida
          password: 9BemGqEa
          port: 22
          script: |
            cd ~/TaskTracker
            git pull
            docker compose down
            docker compose up -d
